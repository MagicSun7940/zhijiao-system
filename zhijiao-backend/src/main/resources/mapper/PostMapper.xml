<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhijiao.mapper.PostMapper">

    <!-- 公开查询：只返回已发布岗位（教师/学生浏览用） -->
    <select id="selectPageWithCondition" resultType="com.zhijiao.entity.Post">
        SELECT * FROM post_info
        WHERE deleted = 0
          AND status = 1
        <if test="courseType != null and courseType != ''">
            AND course_type = #{courseType}
        </if>
        <if test="keyword != null and keyword != ''">
            AND (title LIKE CONCAT('%', #{keyword}, '%')
                OR school_name LIKE CONCAT('%', #{keyword}, '%')
                OR address LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY create_time DESC
    </select>

    <!-- 学校查询：返回本学校所有状态的岗位 -->
    <select id="selectPageBySchool" resultType="com.zhijiao.entity.Post">
        SELECT * FROM post_info
        WHERE deleted = 0
          AND school_id = #{schoolId}
        ORDER BY create_time DESC
    </select>

    <!-- 管理员查询：返回所有岗位（可按状态筛选） -->
    <select id="selectPageForAdmin" resultType="com.zhijiao.entity.Post">
        SELECT * FROM post_info
        WHERE deleted = 0
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
    </select>

</mapper>
